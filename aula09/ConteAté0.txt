algoritmo "ConteAté0"
var
   //Declaração de variáveis
   contador: inteiro
inicio
      //Entrada de dados
      contador <- 10
      
      //Cálculo
      enquanto (contador >= 0) faca
               escreval(contador)
               contador <- contador - 2
      fimenquanto
      escreval("Terminei de contar!")
fimalgoritmo